export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7043', // Secure base URL for development
  // Add httpBaseUrl if some services explicitly need to call http
  // httpApiBaseUrl: 'http://localhost:5057',
  authPath: '/Auth',
  apiPath: '/api'
};
